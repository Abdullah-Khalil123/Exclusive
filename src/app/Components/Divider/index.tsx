import React from "react";

const Divider = () => {
  return (
    <div
      style={{
        height: "1px",
        width: "100%",
        backgroundColor: "var(--grey-color)",
        marginBottom: "30px",
      }}
    ></div>
  );
};

export default Divider;
